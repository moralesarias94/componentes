<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"> Cuenta <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= current_user %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Cerrar sesion", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Iniciar sesion", login_path %></li>
        <% end %>
        <li>
          <%= link_to "Agregar materia", "/home/agregar" %>
        </li>
      </ul>
    </nav>
  </div>
</header>
